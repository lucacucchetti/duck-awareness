name: main

on:
  push:
    branches: [3-add-cicd]

jobs:

  analyze:
    name: Analize
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    steps: 
      - name: Checkout
        uses: actions/checkout@v3
      
      - name: Test
        run: |
          npm run test

      